# Task: Implement Query Parser Component for Metadata Extraction

## Overview
Implement a query parser component that uses LLM to extract metadata from user queries and convert them into structured filters for ChromaDB retrieval. This will enhance the existing RAG system by allowing users to query with natural language that includes specific metadata constraints.

## Current State
- Existing RAG system with RetrievalManager
- Document ingestion with metadata: `rating`, `year`, `month`, `branch`, `reviewer_location`, `original_index`
- ChromaDB integration with collection-based isolation
- LangChain-based query processing

## Goals
1. Extract metadata from natural language queries using LLM
2. Convert extracted metadata to ChromaDB where clause filters
3. Rewrite queries for better semantic search
4. Integrate seamlessly with existing RetrievalManager
5. Handle edge cases gracefully
6. Reuse existing LangChain infrastructure

## Implementation Plan

### Phase 1: Define Query Parser Interface and Models
**File:** `src/disney/rag/query_parser.py`

**Components:**
1. **QueryParserConfig** - Configuration class
2. **QueryParser** - Main parser class
3. **QueryParseResult** - Result data structure
4. **MetadataExtractor** - LLM-based metadata extraction

**Data Structures:**
```python
@dataclass
class QueryParseResult:
    original_query: str
    search_query: str
    filters: Dict[str, Any]
    confidence: float
    extracted_metadata: Dict[str, Any]
    parsing_errors: List[str]

@dataclass
class QueryParserConfig:
    llm_model: str = "gpt-4o-mini"
    temperature: float = 0.1
    max_tokens: int = 500
    confidence_threshold: float = 0.7
    enable_metadata_extraction: bool = True
    enable_query_rewriting: bool = True
```

### Phase 2: Implement Structured Output with Pydantic Models
**File:** `src/disney/rag/query_parser.py` (extend existing)

**Pydantic Models for Structured Output:**
```python
from pydantic import BaseModel, Field
from typing import Optional, Dict, Any

class QueryFilters(BaseModel):
    """ChromaDB filters extracted from user query."""
    rating: Optional[int] = Field(None, ge=1, le=5, description="Customer rating (1-5)")
    year: Optional[int] = Field(None, ge=2000, le=2030, description="Review year")
    month: Optional[int] = Field(None, ge=1, le=12, description="Review month (1-12)")
    branch: Optional[str] = Field(None, description="Disney location: Disneyland, Disney World, or Unknown")
    reviewer_location: Optional[str] = Field(None, description="Reviewer's location")

class QueryParseResult(BaseModel):
    """Structured result from query parsing."""
    search_query: str = Field(..., description="Rewritten semantic query for better search")
    filters: QueryFilters = Field(default_factory=QueryFilters, description="Extracted metadata filters")
    confidence: float = Field(..., ge=0.0, le=1.0, description="Confidence in extraction (0.0-1.0)")
    reasoning: str = Field(..., description="Brief explanation of extraction")

class QueryParserConfig(BaseModel):
    """Configuration for query parser."""
    llm_model: str = Field(default="gpt-4o-mini", description="LLM model to use")
    temperature: float = Field(default=0.1, ge=0.0, le=2.0, description="LLM temperature")
    max_tokens: int = Field(default=500, ge=100, le=2000, description="Maximum tokens")
    confidence_threshold: float = Field(default=0.7, ge=0.0, le=1.0, description="Minimum confidence threshold")
    enable_metadata_extraction: bool = Field(default=True, description="Enable metadata extraction")
    enable_query_rewriting: bool = Field(default=True, description="Enable query rewriting")
```

**Prompt Template:**
```python
METADATA_EXTRACTION_PROMPT = """
You are a query parser for a Disney customer review analysis system. Extract metadata and rewrite queries for better semantic search.

Available metadata fields:
- rating: Integer (1-5) - Customer rating
- year: Integer (e.g., 2023, 2022) - Review year
- month: Integer (1-12) - Review month
- branch: String ("Disneyland", "Disney World", "Unknown") - Disney location
- reviewer_location: String - Reviewer's location
- original_index: Integer - Original review index

User Query: "{query}"

Extract metadata and rewrite the query following these rules:
1. Only include filters for explicitly mentioned metadata
2. If no metadata is mentioned, leave filters empty
3. For ratings, extract the specific number mentioned (1-5)
4. For years, extract 4-digit year format (e.g., 2023)
5. For months, extract month number (1-12)
6. For branches, use exact values: "Disneyland", "Disney World", or "Unknown"
7. For reviewer locations, extract the mentioned location
8. Rewrite search_query to be more semantic and searchable
9. Set confidence based on how clear the metadata extraction is
10. Provide brief reasoning for your extraction

Return the result in the specified structured format.
"""
```

### Phase 3: Implement Core Query Parser with Structured Output
**File:** `src/disney/rag/query_parser.py`

**Key Methods:**
```python
from langchain_openai import ChatOpenAI
from langchain.prompts import ChatPromptTemplate
from langchain_core.output_parsers import PydanticOutputParser
from langchain_core.runnables import RunnablePassthrough

class QueryParser:
    def __init__(self, config: QueryParserConfig):
        self.config = config
        self.llm = ChatOpenAI(
            model=config.llm_model,
            temperature=config.temperature,
            max_tokens=config.max_tokens
        )
        self.prompt_template = ChatPromptTemplate.from_template(METADATA_EXTRACTION_PROMPT)
        
        # Use PydanticOutputParser for structured output
        self.output_parser = PydanticOutputParser(pydantic_object=QueryParseResult)
        
        # Create the chain with structured output
        self.chain = (
            self.prompt_template 
            | self.llm 
            | self.output_parser
        )
    
    async def parse_query(self, query: str) -> QueryParseResult:
        """Parse user query and extract metadata using structured output."""
        if not query or not query.strip():
            return QueryParseResult(
                search_query="",
                filters=QueryFilters(),
                confidence=0.0,
                reasoning="Empty query provided"
            )
        
        try:
            # Use structured output parsing
            result = await self.chain.ainvoke({"query": query.strip()})
            
            # Validate confidence threshold
            if result.confidence < self.config.confidence_threshold:
                logger.warning(f"Low confidence parsing: {result.confidence}")
            
            return result
            
        except Exception as e:
            logger.error(f"Query parsing failed: {e}")
            return self._handle_parsing_error(query, str(e))
    
    def _build_chromadb_filters(self, filters: QueryFilters) -> Dict[str, Any]:
        """Convert Pydantic filters to ChromaDB where clause format."""
        chromadb_filters = {}
        
        if filters.rating is not None:
            chromadb_filters["rating"] = filters.rating
        if filters.year is not None:
            chromadb_filters["year"] = filters.year
        if filters.month is not None:
            chromadb_filters["month"] = filters.month
        if filters.branch is not None:
            chromadb_filters["branch"] = filters.branch
        if filters.reviewer_location is not None:
            chromadb_filters["reviewer_location"] = filters.reviewer_location
            
        return chromadb_filters
    
    def _handle_parsing_error(self, query: str, error: str) -> QueryParseResult:
        """Handle cases where LLM parsing fails."""
        return QueryParseResult(
            search_query=query,
            filters=QueryFilters(),
            confidence=0.0,
            reasoning=f"Parsing failed: {error}"
        )
```

### Phase 4: Integrate with RetrievalManager
**File:** `src/disney/rag/retrieval_manager.py`

**New Methods:**
```python
class RetrievalManager:
    def __init__(self, ..., query_parser: Optional[QueryParser] = None):
        # ... existing init
        self.query_parser = query_parser or QueryParser(QueryParserConfig())
    
    async def query_with_metadata(self, query: str) -> str:
        """Enhanced query method with metadata extraction."""
        # Parse query for metadata
        parse_result = await self.query_parser.parse_query(query)
        
        # Build ChromaDB filters from structured result
        chromadb_filters = self.query_parser._build_chromadb_filters(parse_result.filters)
        
        # Use metadata filters if available
        if chromadb_filters:
            # Use filtered search
            docs = self.vectorstore.similarity_search_with_score(
                parse_result.search_query,
                k=5,
                filter=chromadb_filters
            )
        else:
            # Use regular search
            docs = self.vectorstore.similarity_search_with_score(
                parse_result.search_query,
                k=5
            )
        
        # Process with RAG chain
        return await self._process_with_rag_chain(docs, parse_result.search_query)
    
    def get_enhanced_context(
        self, 
        query: str, 
        n_results: int = 5,
        use_metadata_extraction: bool = True
    ) -> List[Dict[str, Any]]:
        """Enhanced context retrieval with metadata filtering."""
        if not use_metadata_extraction or not self.query_parser:
            return self.get_relevant_context(query, n_results)
        
        # Parse query for metadata
        parse_result = await self.query_parser.parse_query(query)
        chromadb_filters = self.query_parser._build_chromadb_filters(parse_result.filters)
        
        # Use filtered search if metadata available
        if chromadb_filters:
            docs = self.vectorstore.similarity_search_with_score(
                parse_result.search_query,
                k=n_results,
                filter=chromadb_filters
            )
        else:
            docs = self.vectorstore.similarity_search_with_score(
                parse_result.search_query,
                k=n_results
            )
        
        return self._format_context_documents(docs)
```

### Phase 5: Edge Case Handling with Structured Output
**File:** `src/disney/rag/query_parser.py`

**Edge Cases:**
1. **No metadata extracted**: Return empty filters, use original query
2. **Invalid metadata format**: Fallback to original query, log warning
3. **LLM parsing failure**: Use original query, return error details
4. **Confidence too low**: Use original query, log warning
5. **Empty query**: Return error result
6. **Pydantic validation errors**: Handle gracefully with fallback

**Implementation:**
```python
async def parse_query(self, query: str) -> QueryParseResult:
    """Parse user query with comprehensive error handling using structured output."""
    if not query or not query.strip():
        return QueryParseResult(
            search_query="",
            filters=QueryFilters(),
            confidence=0.0,
            reasoning="Empty query provided"
        )
    
    try:
        # Use structured output parsing (handles validation automatically)
        result = await self.chain.ainvoke({"query": query.strip()})
        
        # Validate confidence threshold
        if result.confidence < self.config.confidence_threshold:
            logger.warning(f"Low confidence parsing: {result.confidence}")
            # Still return result but with warning
        
        return result
        
    except ValidationError as e:
        logger.error(f"Pydantic validation error: {e}")
        return self._handle_validation_error(query, str(e))
    except Exception as e:
        logger.error(f"Query parsing failed: {e}")
        return self._handle_parsing_error(query, str(e))

def _handle_validation_error(self, query: str, error: str) -> QueryParseResult:
    """Handle Pydantic validation errors."""
    return QueryParseResult(
        search_query=query,
        filters=QueryFilters(),
        confidence=0.0,
        reasoning=f"Validation error: {error}"
    )

def _handle_parsing_error(self, query: str, error: str) -> QueryParseResult:
    """Handle general parsing errors."""
    return QueryParseResult(
        search_query=query,
        filters=QueryFilters(),
        confidence=0.0,
        reasoning=f"Parsing failed: {error}"
    )
```

### Phase 6: API Integration
**File:** `src/disney/api/routes.py`

**New Endpoint:**
```python
@router.post("/query-enhanced", response_model=QueryResponse)
async def query_reviews_enhanced(
    request: QueryRequest,
    chroma_host=Depends(get_chroma_host),
    chroma_port=Depends(get_chroma_port)
):
    """Enhanced query endpoint with metadata extraction."""
    start_time = time.time()
    
    try:
        logger.info(f"Processing enhanced query: {request.question[:100]}...")
        
        # Initialize RAG components with query parser
        vector_manager = get_retrieval_manager(chroma_host, chroma_port)
        
        # Use enhanced query method with metadata extraction
        answer = await vector_manager.query_with_metadata(request.question)
        
        # Get enhanced context for sources
        context_docs = vector_manager.get_enhanced_context(
            query=request.question,
            n_results=request.context_limit,
            use_metadata_extraction=True
        )
        
        # Format response
        processing_time = (time.time() - start_time) * 1000
        
        return QueryResponse(
            answer=answer,
            sources=context_docs,
            confidence=0.85,  # Could be extracted from parse result
            processing_time_ms=processing_time
        )
        
    except Exception as e:
        logger.error(f"Enhanced query failed: {e}")
        raise HTTPException(status_code=500, detail=str(e))
```

### Phase 7: Configuration and Testing
**File:** `src/disney/shared/config.py`

**New Settings:**
```python
class Settings(BaseSettings):
    # ... existing settings
    
    # Query Parser Settings
    query_parser_enabled: bool = Field(default=True, env="QUERY_PARSER_ENABLED")
    query_parser_llm_model: str = Field(default="gpt-4o-mini", env="QUERY_PARSER_LLM_MODEL")
    query_parser_temperature: float = Field(default=0.1, env="QUERY_PARSER_TEMPERATURE")
    query_parser_confidence_threshold: float = Field(default=0.7, env="QUERY_PARSER_CONFIDENCE_THRESHOLD")
    query_parser_max_tokens: int = Field(default=500, env="QUERY_PARSER_MAX_TOKENS")
```

**Test Files:**
- `tests/test_rag/test_query_parser.py`
- `tests/test_api/test_enhanced_routes.py`

### Phase 8: Usage Examples and Documentation

**Example Usage:**
```python
# Initialize with query parser
config = QueryParserConfig()
parser = QueryParser(config)
manager = RetrievalManager(query_parser=parser)

# Query with metadata extraction
result = await manager.query_with_metadata(
    "What do customers say about Space Mountain at Disneyland in June 2023?"
)
# Automatically extracts: branch="Disneyland", year=2023, month=6
# Rewrites to: "Space Mountain customer reviews experiences"
```

**Example Queries:**
1. "Show me 5-star reviews from Disney World in December 2023"
2. "What complaints do people have about food at Disneyland in summer?"
3. "Find reviews about wait times at Space Mountain from US visitors"
4. "Show me recent reviews about staff friendliness in 2023"
5. "What do people think about the new attraction in March?"

## Implementation Steps

### Step 1: Create Base Structure
1. Create `src/disney/rag/query_parser.py`
2. Add prompt template to `src/disney/rag/prompt_template.py`
3. Update `src/disney/shared/config.py` with new settings

### Step 2: Implement Core Logic
1. Implement `QueryParser` class with Pydantic models
2. Add structured output parsing with LangChain
3. Implement ChromaDB filter building from Pydantic models

### Step 3: Integration
1. Update `RetrievalManager` with query parser integration
2. Add new API endpoint
3. Update existing query methods

### Step 4: Testing
1. Create comprehensive test suite for Pydantic models
2. Test structured output parsing and validation
3. Integration tests with real ChromaDB and metadata filtering

### Step 5: Documentation and Examples
1. Add usage examples
2. Update API documentation
3. Create integration guide

## Success Criteria
1. ✅ Extract metadata from natural language queries
2. ✅ Convert metadata to valid ChromaDB filters
3. ✅ Rewrite queries for better semantic search
4. ✅ Handle all edge cases gracefully
5. ✅ Integrate seamlessly with existing RAG system
6. ✅ Maintain backward compatibility
7. ✅ Comprehensive test coverage
8. ✅ Clear documentation and examples

## Dependencies
- Existing LangChain infrastructure
- LangChain PydanticOutputParser for structured output
- OpenAI API access
- ChromaDB where clause support
- Existing RetrievalManager architecture
- Pydantic for data validation and serialization

## Timeline
- **Phase 1-2**: 2-3 hours (Base structure and prompts)
- **Phase 3-4**: 3-4 hours (Core implementation and integration)
- **Phase 5-6**: 2-3 hours (Edge cases and API)
- **Phase 7-8**: 2-3 hours (Testing and documentation)
- **Total**: 9-13 hours

## Notes
- Use LangChain PydanticOutputParser for reliable structured output
- Maintain backward compatibility with existing query methods
- Focus on robust error handling and graceful degradation
- Leverage Pydantic validation for data integrity
- Consider adding query parsing metrics and monitoring
- Plan for future metadata field extensions
- Structured output eliminates JSON parsing errors and improves reliability
